<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nhbank.ngw.infra.user.mapper.MenuMapper">

    <!-- DB 컬럼 ↔ 도메인 모델 필드 매핑 -->
    <resultMap id="MenuMap" type="com.nhbank.ngw.domain.user.model.Menu">
        <result column="MENU_ID"     property="menuId"/>
        <result column="PARENT_ID"   property="parentId"/>
        <result column="MENU_NAME"   property="menuName"/>
        <result column="IS_LEAF"     property="leaf"/>
        <result column="CREATED_AT"  property="createdAt"/>
        <result column="UPDATED_AT"  property="updatedAt"/>
    </resultMap>

    <sql id="MenuColumns">
        MENU_ID, PARENT_ID, MENU_NAME, IS_LEAF, CREATED_AT, UPDATED_AT
    </sql>

    <select id="selectAll" resultMap="MenuMap">
        SELECT <include refid="MenuColumns"/>
        FROM MENU
        ORDER BY MENU_ID
    </select>

</mapper>