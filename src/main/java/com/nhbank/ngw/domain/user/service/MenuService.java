package com.nhbank.ngw.domain.user.service;

import com.nhbank.ngw.domain.user.model.Menu;

import java.util.List;

public interface MenuService {
    List<Menu> list();              // 부서와 동일하게 단순 조회
}